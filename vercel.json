{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "devCommand": "npm start",
  "installCommand": "npm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/**", "dest": "/index.html" }
  ]
}